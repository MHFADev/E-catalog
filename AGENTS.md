# UMKM Ciangsana Catalog — Project Notes

## Discord laporan (via MCP `mhfadelt-discord`)
- SETIAP laporan yang dikirim harus menyertakan tag `@everyone` (ID server: 1531273740397314110).
- Gunakan mention literal `@everyone` di awal konten pesan.
- Tool yang dipakai: `mhfadelt-discord_send_task_report` / `mhfadelt-discord_analyze_and_advise`.

## Konvensi git
- Branch utama: `main`; ada branch kerja `z`.
- Hanya commit/push jika diminta (`push ke main`, `marge ke main`, dll).
- Pesan commit gaya Conventional Commits (`fix(scope):`, `feat:`, `refactor:`).

## Supabase
- Project: `wqealargyqdxndcrtbla` (db.wqealargyqdxndcrtbla.supabase.co). Project kedua (`ghbegqqtmxrcyorrqbgm`) TIDAK dipakai (tidak ada tabel sellers).